<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Com.Kingdee.App.Views.AboutPage"
             Title="AboutPage">
    <VerticalStackLayout>
        <HorizontalStackLayout Margin="10" Spacing="10">
            <Label
                FontAttributes="Bold"
                FontSize="22"
                Text="About this app"
                VerticalOptions="End"
                />
            <Label FontSize="22"
                Text="v0.1"
                VerticalOptions="End"/>
        </HorizontalStackLayout>
        <HorizontalStackLayout Spacing="10">
            <Label
                FontAttributes="Bold"
                FontSize="22"
                Text="Api Version"
                VerticalOptions="End"
                />
            <Label FontSize="22"
                   Text="{Binding AppVersion}"
                   VerticalOptions="End"
                   />
        </HorizontalStackLayout>
        <Label HeightRequest="60"
               Text="This app is written in xaml and C# with MAUI by Hiuyeung"
               VerticalTextAlignment="Center"
               />
        <Label HeightRequest="60"
               Text="Concept and original design by Robin liberty"/>
        <Label FontSize="Small" Text="Icons from IconScout: https://blog.cuixiaoyang.xyz"></Label>
    </VerticalStackLayout>
</ContentPage>